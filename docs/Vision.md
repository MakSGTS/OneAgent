# OneAgent Vision

OneAgent is a cross-platform AI development platform for 1C:Enterprise.

The Runtime is the core component. MCP, LSP, CLI and IDE integrations are adapters.
The platform works with a semantic configuration model instead of exposing raw XML
and unrestricted filesystem reads to language models.
