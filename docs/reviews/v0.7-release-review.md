# v0.7 Intelligence Release Integration Review

## Decision

**pass**

Recorded on 2026-09-19 (Asia/Novosibirsk). The repeated release review qualifies
the immutable integration head below. R1–R3 from the earlier blocked review are
closed by source evidence and successful macOS/Windows execution. Its two
non-blocking documentation findings, N1 and N2, are corrected in this review
record's documentation-only change. No blocking or non-blocking finding remains
open for this accepted release boundary.

The source review at the integration head returned
`pass with non-blocking follow-ups` for N1/N2. The `pass` recorded here includes
their documentation closure; it does not change that earlier result.

This closes the v0.7 Sprints 36–41 review boundary. It does not claim that a
release has been published: merge to `main`, tag `v0.7`, and their pushes require
separate release authorization. Sprint 42 remains planned until that
step is complete. No broader platform, API stability or Designer XML parity
guarantee is introduced.

## Immutable baseline and method

Reviewed release range:

```text
7adb5230e9bf5a505e442f9e6eec5735a18c188c..b5948d4f0ca9b4877fdb0c4e86196da58370dbe5
```

The base is the committed v0.6 release review. The endpoint is
`Merge Sprint 41 cross-platform remediation`, whose parents are
`5cf9f0995f077b083fc62436b0c103d3c18dbff9` and
`2b9ee1f729075a9e745253b570853c9dc703eb09`. Its tree is identical to that final
remediation head. The merge was pushed to `origin/codex/v0.7` before this
repeat review. The release range changes 192 paths; the remediation relative
to the earlier candidate changes 13 paths.

The [earlier blocked review](https://github.com/MakSGTS/OneAgent/blob/9a74d8c9495db9f986e1b53cac20856c0fc22c90/docs/reviews/v0.7-release-review.md)
remains immutable at `9a74d8c9495db9f986e1b53cac20856c0fc22c90`.
The local branch `codex/v0.7-release-review-blocked-9a74d8c9` preserves it;
the current `codex/v0.7-release-review` was created from the new integration
head. This record supersedes that decision only for the new range, not for
the original failed candidate.

This is a primary release review. It reconciles the committed independent
sprint decisions, the separately completed remediation reviews, current
source boundaries, and fresh release validation. Those earlier independent
reviews retain their own ranges; they are not relabelled as an independent
release review. The final remediation review of
`6f5f075185bc98aeb62cf38a8399b0d86baac716..2b9ee1f729075a9e745253b570853c9dc703eb09`
returned `pass` after the receiver-drop oracle correction.

The repeated review began and ended with a clean working tree at the endpoint.
The documentation closure adds only this record and updates README, Roadmap
and the Rules Engine compatibility wording. Context preflight was `warning`:
effective window and measured token telemetry were unavailable. Inspection
used bounded diffs, symbols, ADR sections and retained logs.

## Acceptance evidence

| Area | Evidence and conclusion |
| --- | --- |
| Diagnostics and rules | [Sprint 36](sprint-36-diagnostics-engine.md), [Sprint 37](sprint-37-rules-engine.md), ADR-0058/0059 and `compose_rule_evidence`: Analysis owns deterministic reports and rule execution; Runtime publishes their complete results. The production rule registry remains empty. |
| Git and publication | [Sprint 38](sprint-38-git-change-adapter.md), ADR-0060 and `production_command`: bounded read-only Git input feeds the source-neutral full rebuild coordinator. Git paths do not become semantic authority. |
| Change impact | [Sprint 39](sprint-39-change-impact-analysis.md), ADR-0061 and `compose_change_impact`: actual adjacent immutable publications feed the canonical Analysis/Graph evaluator. |
| Refactoring planning | [Sprint 40.1](sprint-40-1-refactoring-planner-remediation.md) and ADR-0063 preserve the canonical publication owner, exact source evidence and bounded read-only planning. Sprint 40 administrative closure is not a substitute for that combined review. |
| Safe edits | [Sprint 41](sprint-41-safe-edit-transactions.md), ADR-0064 and the [invariant mapping](../architecture/safe-edit-transactions-invariants.md): opt-in local authority, exact confirmation, joined recovery, complete semantic comparison and reversal remain intact. |
| Product boundary | Runtime/MCP product entry points use the default read-only service; LSP builds a read-only snapshot. No write-capable protocol/UI or Windows mutation support is added. |
| Cache and semantic ownership | Cache schema 1 and semantic compatibility 8 remain; edit authority and undo receipts are not persisted. Graph, Protocol, Tool Policy and EDT extension have no release-range changes. |
| Dependencies | Cargo.lock changes only internal workspace dependency edges. No new third-party package, workspace member, license or mutation backend is introduced. |
| Platform qualification | Exact integration-head CI passes every job and both full Rust pipelines. Portable and non-Unix rejection tests execute on Windows; Unix transaction tests execute on macOS. |
| Documentation | N1/N2 corrections below align current status and API compatibility wording without rewriting historical decisions or expanding scope. |

## Findings closed

| Finding | Resolution and executed evidence |
| --- | --- |
| R1 — canonical temporary root | Scanner positive controls explicitly canonicalize the root. `canonical_root_accepts_and_symlinked_root_rejects` proves acceptance of the canonical directory and rejection of its symlink without weakening production confinement. Both named tests pass in macOS CI. |
| R2 — Windows producer input | `SafeEditProducerInput::new` compares the full expected path in `SourcePath` representation, preserving document/owner correspondence. The Windows Designer test isolates the former native-prefix guard. Disk/UNC positive controls and missing/extra/foreign/mismatched-path negatives pass; both producer projection controls execute successfully on Windows. |
| R3 — platform test boundaries | Unix filesystem/mutation scenarios remain gated. Portable policy, queue/reservation and budget tests run on Windows. Identity/capture rejection and read-only Workspace availability have explicit non-Unix tests; all shared Runtime consumers retain their portable coverage. |
| Portable-oracle follow-up | Five pure blocks were extracted from Unix-only modules. Exact/over/overflow, confirmation/cancellation/redaction and ownership tests execute on both CI operating systems. |
| Receiver-drop P3 | `queued_request_drop_retains_reservation_until_command_drop` awaits confirmed cancellation before checking `Busy`, retains the command until that check and then proves slot release on command drop. The final remediation review passed; this test also passes at the integration head on both platforms. |
| N1 — inherited API wording | [Rules Engine evidence](../architecture/rules-engine-evidence.md#api-dependency-compatibility-and-sensitive-data-audits) now states that `DiagnosticFinding::code()` returns `&DiagnosticCode`, the code is `Clone` rather than `Copy`, repository consumers are migrated, and external ownership-dependent callers may need migration. |
| N2 — README status | README now records completed Sprint 41 and the accepted v0.7 review boundary, with release execution and Sprint 42 explicitly pending. |

## Validation at the integration head

All commands below completed with exit 0 at
`b5948d4f0ca9b4877fdb0c4e86196da58370dbe5`.

| Key | Command | Passed / nonempty harnesses |
| --- | --- | --- |
| F1 | `cargo test -p oneagent-analysis -p oneagent-bsl --all-targets` | 223 / 12 |
| F2 | `cargo test -p oneagent-designer-xml -p oneagent-edt --all-targets` | 401 / 23 |
| F3 | Runtime matrix below | 306 / 13 |
| F4 | `cargo test -p oneagent-tool-policy --all-targets` | 33 / 2 |
| G1 | `cargo fmt --all -- --check` | n/a |
| G2 | `cargo check --workspace --all-targets` | n/a |
| G3 | `cargo test --workspace --all-targets` | 1484 / 83 |
| G4 | `cargo clippy --workspace --all-targets --all-features -- -D warnings` | n/a |
| G5 | `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` | n/a |
| G6 | `git diff --check` | n/a |

```bash
cargo test -p oneagent-runtime --lib \
  --test safe_edit_transactions --test git_change_reader \
  --test git_change_workspace --test repository_change_domain \
  --test workspace_service --test file_watching --test persistent_cache \
  --test mcp_process --test mcp_semantic_tools --test lsp_process \
  --test lsp_stdio --test graph_query_api
```

No focused filter matched zero tests. The full gate has four expected empty
binary harnesses (CLI, Runtime, LSP and MCP), which are not coverage evidence.
Counts overlap across commands and shared fixture consumers; they are executed
tests, not distinct scenario counts. No global `TMPDIR` override was used to
mask the former canonical-root failure.

Local VS Code TypeScript typecheck and compilation passed, followed by 62 unit
tests with zero failures/skips. These used the existing repository-local
Electron Node-mode wrapper; no bundle or signature was modified. Full current
Host/process/package and EDT evidence comes from CI, not a claimed local GUI
rerun. `bash -n scripts/validate-codex-prompts.sh` and the validator passed
(19 prompt files); reviewed-range `git diff --check` also passed.

## Exact-head CI

[CI run 35378273171](https://github.com/MakSGTS/OneAgent/actions/runs/35378273171)
completed successfully with the exact integration `headSha` above.

| Jobs | Result |
| --- | --- |
| Rust macOS (`105708086840`) | All steps pass: Format, Check, Runtime process build, Test, Context/MCP compatibility, Clippy and Rustdoc. Workspace Test: 1484 passed, 83 nonempty harnesses. |
| Rust Windows (`105708086856`) | The same complete pipeline passes. Workspace Test: 1399 passed, 83 nonempty harnesses. |
| VS Code macOS and Windows | Both jobs and every step pass, including extension, public Runtime process and package/scope checks. |
| EDT macOS and Windows | Both jobs and every step pass, including real Runtime verification and p2 package audit. |
| Prompt contracts | Passed. |

Raw status, platform logs, per-oracle qualification, baseline and local command
ledgers are retained under the ignored directory
`local-artifacts/codex-runs/v0.7-release-review-repeat/`. In particular,
`ci-qualification.json` records nonzero exact-name evidence for the corrected
Windows producer, portable Runtime, non-Unix rejection and retained macOS
confinement/transaction oracles. No skipped Rust step or different SHA is used
as qualification. The documentation closure does not relabel these baseline
executions as tests of its later documentation commit.

## Preserved limitations and next action

There is no missing mandatory evidence for the accepted review boundary.
Linux execution and renewed pinned Codex/Cursor execution were not performed;
neither broader platform coverage nor new exact-client compatibility is claimed.
The v0.6 external-client evidence remains historical. Windows mutation remains
unsupported. Unix edits assume cooperative external writers and do not promise
crash-atomic multi-file replacement, durable undo, hostile-writer protection,
ACL/xattr preservation, or recovery from process abort/OOM. Cancellation remains
cooperative; global panic-hook output is outside the accepted redaction claim.
Designer XML parity remains v0.8/v0.9 scope.

This authorized documentation closure records `pass`, corrects N1/N2, and marks
the v0.7 review boundary completed in Roadmap. Historical review decisions,
source/tests, dependencies, prompt inventories and `.codex/` are unchanged.
After documentation validation, commit/push and no-ff review integration,
require green CI for that integration head before proposing the separately
authorized `main` merge and `v0.7` tag. No release publication or Sprint 42
kickoff is part of this change.
